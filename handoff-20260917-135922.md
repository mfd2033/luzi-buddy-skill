# Handoff: luzi-skill —— 炉子(luzi.top) Agent API Skill 的创建与能力验证

## Current state
- **目标达成一半**：已创建一个 Agent 技能（`luzi-skill`，与具体 Agent 工具无关，任意支持 Skill 机制的 Agent 均可使用），封装炉子 Agent API，并建好公开仓库、克隆、提交、推送。
- **GitHub 仓库**：`https://github.com/mfd2033/luzi-skill`（**公开**，owner=`mfd2033`）。克隆于 `D:\github\luzi-skill`，分支 `main`。
- **已验证的能力（用真实 token 实测返回 200 + 真实数据）**：
  - 资产读取 `GET /api/v1/assets/`
  - 随手记读取 `GET /api/v1/notes/`
  - 标签读取 `GET /api/v1/notes/tags` 与 `GET /api/v1/tags/`
  - 配方读取 `GET /api/v1/recipes/`
  - 搜索读取 `GET /api/v1/search`
  - 浏览广场 `GET /api/v1/community/plaza/publications`（端点正确，但未用 token 单独实测）
- **API 事实**：Base URL = `https://app.luzi.top/api/v1/`；认证 = `Authorization: Bearer $LUZI_AGENT_TOKEN`。
- **仓库文件**：`SKILL.md`、`luzi.ps1`（PowerShell 助手）、`README.md`、`.env.example`、`.gitignore`。
- **关键决策 / 安全**：token **绝不进仓库**。仅通过环境变量 `LUZI_AGENT_TOKEN` 或本地 `.env`（已被 `.gitignore` 忽略）提供。仓库只含 `.env.example`。
- **未验证能力（8 项，会改/未知端点，未擅自操作）**：资产写入、随手记写入、配方写入、标签写入、文件读取、文件上传、从广场存副本、部署材料。写操作路径按 REST 约定是 `POST` 对应基路径，但未经实测；文件/上传/存副本/部署材料的真实端点尚未确定（早期路径猜测 `/files`、`/file-objects`、`/square`、`/deploy` 均 404）。

## Next steps（按优先级）
1. **只读确认「浏览广场」**：用 `LUZI_AGENT_TOKEN` 跑一次 `GET /api/v1/community/plaza/publications`，把结果补进 `SKILL.md` 的「已验证」表（去掉「端点正确但未实测」的标注）。零风险。
2. **补齐 4 个未知端点**（文件读取 / 文件上传 / 从广场存副本 / 部署材料）：用 `browser-skill` 打开对应炉子 Web 界面（app.luzi.top），触发操作并抓网络请求，得到真实 URL 与方法，然后更新 `luzi.ps1` 与 `SKILL.md`。
3. **可回滚写入验证**（4 个写入能力）：对每个写能力做「创建测试项 → 确认 200 → 立即删除」的安全测试，把验证过的端点与请求体写进 skill。**需用户授权**，因为会改动其账号数据。
4. **丰富 `luzi.ps1`**：增加写入/上传/存副本/部署子命令，并支持参数化（如资产名、文件路径）。
5. **（可选）发布 skill**：若要在 Agent 技能市场使用，按对应技能目录规范放置（本仓库根目录已有 `SKILL.md`）。

## Relevant artifacts
- `SKILL.md` — skill 定义、已/未验证能力表、用法（本仓库根目录）
- `luzi.ps1` — 调用助手，支持 `assets|notes|tags|tagsall|recipes|search|plaza`
- `README.md` — 仓库说明与安全提醒
- 仓库：`https://github.com/mfd2033/luzi-skill`
- 原始线索：炉子官网 `https://www.luzi.top/#top`；Web 版 `https://app.luzi.top`；Agent 管理在桌面客户端（Web 端仅展示）
- 早期抓取的 JS 包位于 `d:\workspace\luzi\luzi-bundle.js`（用于反查前端接口，可选参考）

## Suggested skills
- `browser-skill` — 抓 UI 网络请求以确定文件读取/上传、从广场存副本、部署材料的真实端点（第 2 步必需）
- `handoff` — 若本会话仍需再次交接
- （写能力验证涉及改数据，建议先与用户确认范围，不属于特定 skill）

## 注意事项
- 仓库是**公开**的：任何提交都不要包含真实 `LUZI_AGENT_TOKEN` 或 `.env`。
- 炉子 Web 端 `/agent` 路由为 404，Agent 管理为桌面端专用；API 才是 agent 凭证的真正出口。
- 系统自带 `curl.exe` 对 api.github.com 报 SSL 错误（exit 35），但 PowerShell `Invoke-RestMethod` 可用 —— 若需在脚本里调 GitHub API，用后者。
